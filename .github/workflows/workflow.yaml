name: Download files from GTA5-mods

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  download:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Setup Git LFS
        run: |
          git lfs install
          git lfs track "*.rar"
          git lfs track "*.zip"
          git lfs track "*.7z"
          git add .gitattributes

      - name: Download files
        run: |
          mkdir -p mods

          curl --globoff -L "https://files.gta5-mods.com/uploads/all-police-station-open/f5186e-AllPoliceStationOpen.zip" -o "mods/policestation.zip"

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add .
          git commit -m "Add GTA5 mods files" || echo "No changes to commit"

          git push
